var GameText = /** @class */ (function () {
    function GameText(content, author) {
        this.content = content;
        this.author = author;
    }
    return GameText;
}());
export default GameText;
